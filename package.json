{
  "name": "algoritmos-y-complejidad",
  "version": "1.0.0",
  "description": "Ayudantías de algoritmos y complejidad",
  "scripts": {
    "start": "marp --server --theme beam.css src",
    "html": "marp --html --theme beam.css src/**/*.md",
    "pdf": "marp --pdf --pptx-editable --theme beam.css src/**/*.md",
    "pptx": "marp --pptx --theme beam.css src/**/*.md"
  },
  "dependencies": {},
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.2"
  }
}
